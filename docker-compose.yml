services:
  arket:
    build: .
    ports:
      - "3000:3000"
    environment:
      SOLANA_RPC_URL: ${SOLANA_RPC_URL:-}
      ARKET_DB_PATH: /app/apps/arket/.data/arket.sqlite
    volumes:
      - arket-data:/app/apps/arket/.data
    restart: unless-stopped

volumes:
  arket-data:
